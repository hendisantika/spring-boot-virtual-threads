thread-mode: virtual

spring:

  datasource:
    url: jdbc:postgresql://localhost:6000/postgres?currentSchema=library
    username: postgres
    password: postgres
  sql:
    init:
      platform: postgres
  threads:
    virtual:
      enabled: true

server:
  port: '8081'
